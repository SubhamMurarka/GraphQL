version: '3.8'

services:
# redis:
  #   image: redis:latest
  #   container_name: redis_service_021
  #   restart: always
  #   ports:
  #     - "6399:6379"
  #   networks:
  #     - mynetwork

  backendserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backendserver
    ports:
      - "8085:8081"
    # depends_on:
    #   - redis
    env_file:
      - .env
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
